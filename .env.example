# EmailJS credentials used by the contact form (https://dashboard.emailjs.com).
# These are public by design — they ship in the client bundle. Lock the service
# down with the allowed-origins setting in the EmailJS dashboard.
VITE_APP_SERVICE_ID=
VITE_APP_TEMPLATE_ID=
VITE_APP_PUBLIC_KEY=
